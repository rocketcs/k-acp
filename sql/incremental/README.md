# 必读：本目录已弃用（仅保留一个月过渡）

> 本目录下所有 SQL 脚本**已弃用**，请勿再基于本目录新增脚本。

## 升级机制已切换为 Flyway

项目数据库升级机制自引入 Flyway 后已变更，且 Flyway 的管理范围**不包含本目录脚本**：

- **首次拉取代码（新库）**：直接执行仓库根目录 `sql/db_init.sql` 完成初始化即可，无需执行本目录任何脚本（db_init.sql 已包含全量表结构与初始数据）；
- **后续升级**：由 Flyway 自动完成，增量脚本统一放在 `runner-console/src/main/resources/db/migration/` 下（命名 `V2__xxx.sql` 起），`runner-console` 启动时自动按序执行。

## 保留原因（仅过渡期使用）

本目录暂时保留，是用于**存量环境**（非首次部署，数据库已存在旧结构）的升级过渡：这类环境需根据实际情况**选择性执行**本目录下的脚本，以补齐数据库结构与代码版本的差异；首次拉取代码的新环境不需要执行本目录脚本。

## 清理计划

本目录仅保留**一个月**作为过渡期，到期后将整体删除。请务必在过渡期内完成代码更新。
