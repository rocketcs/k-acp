# 格式建议

- Word：标题分级、正文适度间距，Markdown 表格转换为原生 Word 表格并加粗表头。
- Excel：表格写入真实单元格；金额、数量、日期保持对应类型；冻结表头并限制过宽列。
- PDF：表格绘制外框和列线，长表重复表头；中文使用 TrueType 字体，避免 CFF 字体解析错误。
- 下载：只使用工具返回的 `download_url`，不要猜测附件 ID 或输出本地路径。
